<?xml version="1.0" encoding="utf-8"?>

<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModels="clr-namespace:Grocery.App.ViewModels"
             xmlns:m="clr-namespace:Grocery.Core.Models;assembly=Grocery.Core"
             x:Class="Grocery.App.Views.ProductCategoryView"
             x:DataType="viewModels:ProductCategoryViewModel"
             Title="ProductCategoriesView">
    
    <ContentPage.Content>
        <Grid>

            <Grid.RowDefinitions>
                <RowDefinition />
                <RowDefinition />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="4*"/>
                <ColumnDefinition Width="2*"/>
            </Grid.ColumnDefinitions>
            
            <Border 
                Stroke="#C49B33" 
                StrokeThickness="4" 
                Padding="10" 
                Margin="10" 
                HorizontalOptions="Center"
                >
                
                <CollectionView 
                    ItemsSource="{Binding Products}"
                    >
                    
                    <CollectionView.ItemsLayout>
                        <LinearItemsLayout Orientation="Vertical" ItemSpacing="10"/>
                    </CollectionView.ItemsLayout>
                    
                    <CollectionView.Header>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Label Grid.Column="0" Text="#" FontAttributes="Bold" HorizontalOptions="End"></Label>
                            <Label Grid.Column="1" Text="Naam" FontAttributes="Bold" HorizontalOptions="End"></Label>
                        </Grid>
                    </CollectionView.Header>
                    
                    <CollectionView.ItemTemplate>
                        <DataTemplate x:DataType="m:Product">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Label Grid.Column="0" Text="{Binding Id}"/>
                                <Label Grid.Column="1" Text="{Binding Name}"/>
                            </Grid>
                        </DataTemplate>
        
                    </CollectionView.ItemTemplate>
                </CollectionView>
            </Border>
            
            <!-- <Border Grid.Row="1" Grid.Column="1" Stroke="#C49B33" StrokeThickness="4" Padding="5" Margin="5" HorizontalOptions="Center"> -->
            <!-- <StackLayout> -->
            <!--     <SearchBar x:Name="searchBar" SearchCommand="{Binding PerformSearchCommand}" SearchCommandParameter="{Binding Text, Source={x:Reference searchBar}}"/> -->
            <!--     <CollectionView  -->
            <!--         ItemsSource="{Binding AvailableProducts}"  -->
            <!--         Margin="5" WidthRequest="300" HorizontalOptions="Start" -->
            <!--         SelectionMode="Single" -->
            <!--         SelectionChangedCommand="{Binding AddProductCommand}" -->
            <!--         SelectionChangedCommandParameter="{Binding Source={RelativeSource Self}, Path=SelectedItem}"> -->
            <!--         <CollectionView.ItemsLayout> -->
            <!--             <LinearItemsLayout Orientation="Vertical" ItemSpacing="10" /> -->
            <!--         </CollectionView.ItemsLayout> -->
            <!--         <CollectionView.ItemTemplate> -->
            <!--             <DataTemplate x:DataType="m:Product"> -->
            <!--                 <Grid> -->
            <!--                     <Grid.ColumnDefinitions> -->
            <!--                         <ColumnDefinition Width="3*"/> -->
            <!--                         <ColumnDefinition Width="*"/> -->
            <!--                     </Grid.ColumnDefinitions> -->
            <!--                     <Label Grid.Column="0" Text="{Binding Name}" VerticalOptions="Center"/> -->
            <!--                     <Label Grid.Column="1" Text="{Binding Stock}" VerticalOptions="Center" HorizontalOptions="End" Margin="0,0,10,0"/> -->
            <!--                 </Grid> -->
            <!--             </DataTemplate> -->
            <!--         </CollectionView.ItemTemplate> -->
            <!--         <CollectionView.EmptyView> -->
            <!--             <ContentView> -->
            <!--                 <VerticalStackLayout HorizontalOptions="Center" VerticalOptions="Center"> -->
            <!--                     <Label Text="Er zijn geen producten meer om toe te voegen" FontAttributes="Bold" HorizontalTextAlignment="Center"/> -->
            <!--                 </VerticalStackLayout> -->
            <!--             </ContentView> -->
            <!--         </CollectionView.EmptyView> -->
            <!--     </CollectionView> -->
            <!-- </StackLayout> -->
        <!-- </Border> -->
        </Grid>
    </ContentPage.Content>
</ContentPage>